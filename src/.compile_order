# VHDL compilation order for: src
# Generated by scan_project.sh — edit freely, never overwritten by rescan.
#
# List one filename per line (no directory path), in compilation order.
# Rule: a package or entity must appear BEFORE any file that uses it.
#   e.g. if top.vhd instantiates counter, list counter.vhd first.
# Lines starting with # and blank lines are ignored.
#
# Order per README: cdc_pkg first, cdc_sync_bit and dpram before their users.
cdc_pkg.vhd
cdc_sync_bit.vhd
cdc_sync_pulse.vhd
cdc_handshake.vhd
dpram.vhd
cdc_async_fifo.vhd
cdc_pingpong.vhd
